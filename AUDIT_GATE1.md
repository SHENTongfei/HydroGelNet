# AUDIT GATE-1 — 方案独立审计（SIMPLEX 水凝胶组成→粘附强度）

## 审计人视角：批判性找茬，非复述方案

### 1. 数据泄漏检查
- [x] 内部集（df_180）与外部集（df_341−df_180）无行重叠（按组成精确匹配剔除）
- [x] 缩放/插补在 fold 内完成（trainer Preprocessor），测试集不参与
- [x] 外部集仅在最终评估一次，不参与调参（tuner 只跑内部 CV）
- **风险**：df_341 与 df_180 来自同一实验室同一批实验流程 → "外部集"严格说是**时间外推**（SMBO 后期批次），不是完全独立队列。审稿人可能质疑"同一实验室数据算不算外部验证"。
  - 缓解：论文明确表述为 "temporal extrapolation to SMBO-discovered high-performance region"，并强调这是**比随机划分更难的评估协议**（RF/Ridge 全部 R²<0）。这是卖点而非缺陷。

### 2. 循环论证检查
- [x] 目标（Glass 粘附强度）是实测值，不是模型输出的函数
- [x] X（6 单体摩尔分数）是配方输入，非测量响应
- [x] 无双侧测量（无"原因侧+结果侧"同时作为特征）
- **通过**：composition→property 无循环论证

### 3. 红牌复查（red_card_cases §5）
| # | 检查 | 结果 |
|---|---|---|
| 1 | Y 有闭式公式？ | 无（粘附强度无解析模型）PASS |
| 2 | 参数从 X 读出？ | 无公式，N/A PASS |
| 3 | Y 从曲线提取？ | 否，Y 是实测标量 PASS |
| 4 | Ridge ≥0.95？ | 内部 0.44 / 外部 -1.18，远未压缩 PASS |
| 5 | 单设备指纹？ | 多配方空间，非单台设备 PASS |
| 6 | X 有组合自由度？ | 6 单体 simplex，组合自由 PASS |
| 7 | 外部 n≥30？ | n=161 PASS |
| 8 | 最近 2 年撞车？ | ADJACENT（Nature 单目标 RFR/GP；我们深度多目标+外推）|

### 4. 样本量支撑
- [x] 内部 180 训练 + 外部 161 测试：比 TransMICRO（18 训练/n=3 外部）强一个数量级
- [x] 6 维输入低维，180 样本可支撑中等容量模型
- **风险**：Transformer（n_blocks=2-4, d_model=96-192）在 180 样本上可能过拟合 → 需强正则（dropout/SAM/EMA/Mixup）+ 对比预训练

### 5. 创新点真伪
- [x] 组合外推协议：真实（RF 都崩，任务确实难）
- [x] 双模态组成编码（分数+两两交互）：真实且可消融
- [x] 炫技组合：Transformer+注意力+FiLM+ModalityGate+对比+MFM+SWA+SAM+EMA+不确定性+域约束，全部任务相关可消融
- **注意**：需在消融中证明每项技术增益（H13），无效即删

### 6. 结论
**PASS（有条件）**：方案无硬伤，数据无泄漏无循环论证，红牌全绿。
**条件**：① 外部集表述必须用"时间外推"而非"独立队列"；② 消融必须证明炫技增益；③ 若内部 CV R² < RF（0.70）需升级（L1 借力 → L4 换模型族）。

## 整改动作（审计后）
1. DATA_SOURCES.md 注明外部集为同实验室 SMBO 时间外推批次
2. 论文将外部评估表述为 "out-of-distribution extrapolation benchmark"
3. 保留 df_316 作为中间规模消融（数据量敏感性）
